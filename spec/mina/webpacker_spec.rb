require "spec_helper"

RSpec.describe Mina::Webpacker do
  it "has a version number" do
    expect(Mina::Webpacker::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
